[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["gym_mkds"]

[project]
name = "gym_mkds"
version = "0.0.1"
dependencies = [
  "gymnasium",
  "py-desmume-mkds",
  "numpy",
  "pre-commit",
]
