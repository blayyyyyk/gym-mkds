[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["gym_mkds"]

[project]
name = "gym_mkds"
version = "0.1.1"
dependencies = [
  "gymnasium",
  "py-desmume-mkds>=0.0.15",
  "numpy",
  "pre-commit",
]
